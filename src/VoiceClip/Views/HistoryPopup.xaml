<Window x:Class="VoiceClip.Views.HistoryPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VoiceClip.Views"
        xmlns:vm="clr-namespace:VoiceClip.ViewModels"
        mc:Ignorable="d"
        Title="VoiceClip History"
        Width="400" Height="500"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        Deactivated="Window_Deactivated">

    <Border Background="White"
            CornerRadius="8"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Margin="16,12,16,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="VoiceClip History"
                           FontSize="16" FontWeight="SemiBold"
                           Foreground="#333333"/>
                <Button Grid.Column="1" Content="X"
                        Width="24" Height="24"
                        BorderThickness="0" Background="Transparent"
                        Foreground="#999999" FontSize="14"
                        Cursor="Hand"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- Search Bar -->
            <TextBox Grid.Row="1"
                     Margin="16,0,16,8"
                     Padding="8,6"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     Tag="Search entries..."
                     FontSize="13"
                     BorderBrush="#CCCCCC"/>

            <!-- History List -->
            <ListBox Grid.Row="2"
                     Margin="16,0,16,8"
                     ItemsSource="{Binding Entries}"
                     SelectedItem="{Binding SelectedEntry}"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="{Binding Preview}"
                                       TextTrimming="CharacterEllipsis"
                                       FontSize="13" Foreground="#333333"/>
                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="{Binding Timestamp, StringFormat='{}{0:g}'}"
                                       FontSize="11" Foreground="#999999"/>
                            <TextBlock Grid.Row="1" Grid.Column="1"
                                       Text="{Binding DurationSeconds, StringFormat='{}{0:F1}s'}"
                                       FontSize="11" Foreground="#999999"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding CopyCommand}"
                                CommandParameter="{Binding SelectedEntry}"/>
                </ListBox.InputBindings>
            </ListBox>

            <!-- Bottom Bar -->
            <StackPanel Grid.Row="3" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="16,0,16,12">
                <Button Content="Copy Selected"
                        Padding="12,6" Margin="0,0,8,0"
                        Command="{Binding CopyCommand}"
                        CommandParameter="{Binding SelectedEntry}"/>
                <Button Content="Delete"
                        Padding="12,6" Margin="0,0,8,0"
                        Command="{Binding DeleteCommand}"
                        CommandParameter="{Binding SelectedEntry}"/>
                <Button Content="Clear All"
                        Padding="12,6"
                        Command="{Binding ClearAllCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
