<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VoiceClip.Controls"
                    xmlns:models="clr-namespace:VoiceClip.Models">

    <DataTemplate x:Key="DictationEntryItemTemplate" DataType="{x:Type models:DictationEntry}">
        <Grid Margin="0,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="{Binding Preview}"
                       TextTrimming="CharacterEllipsis"
                       FontSize="13" Foreground="#333333"
                       Margin="0,0,0,2"/>
            <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="{Binding Timestamp, StringFormat='{}{0:g}'}"
                       FontSize="11" Foreground="#999999"/>
            <TextBlock Grid.Row="1" Grid.Column="1"
                       Text="{Binding DurationSeconds, StringFormat='{}{0:F1}s'}"
                       FontSize="11" Foreground="#999999"
                       Margin="8,0,0,0"/>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
